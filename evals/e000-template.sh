#!/usr/bin/env bash
# eval: <what recurring failure this encodes>
# date: YYYY-MM-DD | failure: ... | root cause: ... | patch: ... | next: ...
set -euo pipefail
cd "$(dirname "$0")/.."

# Replace with a real check. Must fail while the problem exists.
echo "PASS: template eval (replace me)"
exit 0
