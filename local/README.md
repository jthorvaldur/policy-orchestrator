# Local directory

This directory is for local-only files that should not be committed.
Use it for machine-specific configuration, local state, or temporary data.

Contents of this directory (except this README and .gitkeep) are gitignored.
